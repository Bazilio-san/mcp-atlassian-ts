Надо придумать, как сделать переклчатель режима работы инструмента jira_create_issue
src/domains/jira/tools/core/jira_create_issue.ts

У меня есть микросервис получения точного логина по не точно указанному имени.

Параметр assignee и reporter сейчас ожидают точный логин.

Альтернативный вариант - это получать в эти параметры не обязательно точное ABJ или неточный логин или email.
Далее -дернуть микросервис. Отправить в него полученное значение и,
если он вернет единственный объект с логином, использовать этот логин в качестве assignee (reporter)
А если вернется несколько объектов - то есть есть выбор, то вернуть ответ с ошибкой и сообщением о необходимости выбора.
Пользоваетль ответит, и llm в следующий раз прищлет точное значение в качестве параметра assignee (reporter)

Я вот хочу, чтобы ты придумал, как сделать такой функционал pluggable:
То есть, если в настройках указывается микросервис получениея точных данных по неточному ФИО/логину,
то тогда применяется альтернативный вариант  

Опиши решение этой задачи.
Подробно. Что где нужно поменять или добавить.

